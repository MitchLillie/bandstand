# --- Node / TypeScript ---
node_modules/
dist/
build/
*.tsbuildinfo
coverage/
.npm/
.pnpm-store/

# --- Editors / OS junk ---
.DS_Store
*.log
.idea/
.vscode/

# --- Python (legacy port; remove once band.py/cli.py are gone) ---
__pycache__/
*.pyc

# --- Secrets / local state — NEVER commit ---
.env
.env.*
.band_session.json
.band_config.json

# --- Captured traffic & private data (kept in ../band-api-private) ---
*.har
hars/
report.md
ventura_*.json
